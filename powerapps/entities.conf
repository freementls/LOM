# EntityName|row_selector|parent_selector|id_attr|columns
# Native child chains use '/' (e.g. region/zone/entity). '_' is allowed inside tag names.
# columns: @attr for attributes, tagname for child text nodes
# Power Apps: call /api/ListPeople?$filter=...&$top=50 — server filters; avoids 500/2000 local limit.
People|person|big_container|lom_id|@age,@eye_color,name,lastname,hobby
